"""Shared pytest fixtures for the ch02 test suite.

Fixtures land alongside their consuming PR:
- `env_or_skip` ships with PR 2 (src/ch02/env.py).
- `tiny_dataset` ships with PR 4 (src/ch02/dataset.py).
"""
